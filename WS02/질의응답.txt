1. 모든 코드 sdds로 네임스페이스 감싸는 이유.
2. 포인터 int* , (*int) 차이
3. 첫번째 load 함수에서
// if문 안에 read함수 사용시 적용??
// read(stduent.m_name); 로컬 변수를 사용하는 이유? 123자인지 확인하기 위해?
// 동적할당을 name에만 해주는 이유?
3. line 42 포인터 쓰는 이유
4. dellocateMemory()함수 설명 왜 멤버별로 하고 구조체하는지